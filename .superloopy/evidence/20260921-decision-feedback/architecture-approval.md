# ROOT selection

Approve candidate A only: deepen AndroidDiagnosticExporter to own one native
document-picker registration/result/retirement path shared by product and native
instrumentation. This removes duplicated lifecycle orchestration and lets real
Android evidence cross the same Interface as production. No release/metrics
refactor: those Modules already concentrate their policies.

Conditions: original physical Activity/WebView Adapter supplies a current-origin
observation captured from itself, never global replacement lookup. Foreground
required when starting picker; ordinary external-picker pause must not cancel it.
Retirement rejects old invocation and late callbacks cannot target a new owner.
Keep native-only URI, fixed type/name, no JS arguments, single Save/Share lease,
bounded worker/write timeout, close-before-saved, normal cancel, no automatic
retry and no deletion of user-selected documents. Existing Share unchanged.
No new generic storage Interface or dependency. Parent owns all CI validation.

Ownership approved only for candidate's four listed files; update scoped report
and related tests as needed. Ask ROOT if a material new choice appears.
