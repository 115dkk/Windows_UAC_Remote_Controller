# UAC 원격 승인 {{VERSION}} 프리릴리즈

## 이번 변경

- **승인 창의 생김새를 기록합니다. 창에 적힌 글자는 기록하지 않습니다.** 휴대폰에 프로그램 이름과 게시자와 실행될 명령어를 따로 보여 주려면 그 값들이 창의 어느 자리에 있는지 알아야 합니다. 그런데 승인 창은 보안 데스크톱에 떠 있어서 이 제품의 탐침 말고는 무엇도 들여다볼 수 없습니다. 그래서 요소의 순서와 깊이, 종류, 자동화 id, 클래스 이름만 Windows 이벤트 로그에 남깁니다. 프로그램 이름도 게시자도 파일 위치도 남기지 않습니다. 이 기록을 읽어야 다음 판에서 그 세 가지를 따로 보여 줄 수 있습니다.
- **휴대폰의 승인 단추가 회색으로 굳어 다시는 승인할 수 없게 되던 문제를 고쳤습니다.** 거절은 되는데 승인만 안 되고, 앱을 완전히 닫았다 열기 전에는 풀리지 않았습니다. 승인 한 번이 끝난 뒤 뒷정리가 실패하면 그 자리가 계속 잡혀 있었고, 그 자리를 놓아 주는 재시도는 앱을 내릴 때만 돌았습니다. 이제 평소에도 정해진 횟수만큼 다시 시도합니다. 이미 끝난 정리를 되풀이하지는 않습니다.
- **요청 하나가 실패했다고 승인 기능 전체가 멈추던 구조를 고쳤습니다.** 연결 하나의 처리나 정리 한 번이 실패하면 그 하나만 잃고 계속 갑니다. 연속으로 네 번 넘게 실패하면 그때는 예전처럼 멈춥니다. 어떤 오류가 정말 전체를 끝내는 것인지 이제 한 곳에 적혀 있고, 나중에 새 오류가 생기면 분류하지 않고는 빌드가 되지 않습니다.
- 승인 창이 파일 위치를 알려 주지 않는 요청에서 휴대폰 화면에 '실행 파일' 항목이 빈 채로 나오던 것을 고쳤습니다. 이제 그 줄을 아예 그리지 않습니다.
- 앱 내부 이름을 알아보기 어렵게 바꾸던 처리를 껐습니다. 소스가 공개되어 있어 가릴 것이 없고, 정작 핵심인 네이티브 부분은 그 처리가 닿지도 않습니다. 대신 문제가 생겼을 때 어디서 났는지가 바로 읽힙니다. 미사용 코드 제거는 그대로입니다.
- 아래 항목은 이전 프리릴리즈의 변경 사항으로 계속 포함됩니다.

- **PC에서 승인 창이 떴을 때 휴대폰이 알림을 띄우지 못하고 멈추던 문제를 고쳤습니다.** 이것이 지금까지 휴대폰 쪽 승인 처리가 첫 요청에서 죽던 원인입니다.
- Windows 승인 창은 프로그램의 파일 위치를 항상 보여 주지는 않습니다. 설치 관리자나 일부 형태의 프로그램에서는 위치가 비어 있고, 이는 규격상 정상입니다. 그런데 휴대폰이 그 값을 잘못된 요청으로 받아 알림 만들기를 거부했고, 그 거부가 승인 처리 전체의 종료까지 번졌습니다. 이제 위치가 없는 요청도 정상으로 받고, 알림에는 프로그램 이름만 표시합니다.
- 진단 다섯 판을 거쳐 실제 휴대폰에서 자리를 한 줄까지 좁혀 찾았습니다. 그 과정에서 넣은 기록은 그대로 남아 있어, 앞으로 같은 종류의 고장은 처음부터 어디서 났는지 말해 줍니다.
- 아직 남은 문제가 있습니다. 알림 하나를 띄우지 못한 것과 승인 처리가 끝난 것을 여전히 같은 일로 다룹니다. 이번 판은 실패하는 원인 하나를 없앴을 뿐이고, 다른 이유로 알림이 실패하면 같은 일이 일어납니다. 다음 판에서 이 구분을 넣습니다.
- 아래 항목은 이전 프리릴리즈의 변경 사항으로 계속 포함됩니다.

- 이 판도 진단판입니다. 동작은 바뀌지 않았습니다.
- 지난 판으로 실패한 이유의 종류를 알아냈습니다. **알림을 만드는 과정이 요청의 남은 시간을 거부합니다.** 남은 시간이 5분 이내여야 그림을 그리는데, 실제 요청이 그 조건을 통과하지 못합니다. 그런데 PC가 싣는 요청 시간은 110초이고 규격 상한도 120초여서, 이 값이 5분을 넘을 수 있는 경로가 코드상으로는 보이지 않습니다.
- 그래서 이번 판은 거부된 그 숫자를 기록합니다. 숫자가 터무니없이 크면 시간을 세는 기준이 어긋난 것이고, 5분을 조금 넘는 정도면 경계 자체가 실제 요청보다 짧게 잡혀 있는 것입니다. 고칠 자리가 둘 중 어느 쪽이냐에 따라 달라지므로, 추측으로 고치지 않고 먼저 잽니다.
- 기록하는 것은 남은 시간(밀리초) 하나뿐입니다. 요청 화면에 이미 초 단위로 보이는 값과 같은 것이며, 요청 내용은 아무것도 남기지 않습니다.
- 다음 판에서 실제 수정을 넣습니다.
- 아래 항목은 이전 프리릴리즈의 변경 사항으로 계속 포함됩니다.

- 이 판도 진단판입니다. 동작은 바뀌지 않았습니다.
- 지난 판의 기록으로 실제 휴대폰에서 자리를 한 줄까지 좁혔습니다. PC에서 진짜 승인 창을 띄우면 **휴대폰이 알림을 띄우는 데 실패하고, 그 실패 하나가 승인 처리 전체의 종료로 번집니다.**
- 그런데 실패한 이유가 바로 그 자리에서 버려지고 있었습니다. 알림을 띄우는 과정이 어떤 오류를 만나든 이유를 지우고 한 가지 값으로 바꾼 뒤, 알림 관리 부분을 영구히 사용 불가로 표시했습니다. 이제 버려지던 이유에 이름을 남깁니다. 남기는 것은 오류의 종류와 만들어진 파일·줄 세 군데까지이고, 오류 문구 자체는 값이 섞여 들어갈 수 있으므로 남기지 않습니다.
- 다음 판에서 이 이름을 읽어 실제 수정을 넣습니다. 알림 하나를 못 띄운 것과 승인 처리가 끝난 것은 같은 일이 아니며, 그 구분이 다음 판의 내용입니다.
- 아래 항목은 이전 프리릴리즈의 변경 사항으로 계속 포함됩니다.

- 이 판도 진단판입니다. 동작은 바뀌지 않았습니다.
- 지난 판의 기록으로 실제 휴대폰에서 자리를 좁혔습니다. PC에서 진짜 승인 창을 띄운 지 2초 만에 휴대폰 쪽이 멈추는데, 승인 처리가 예기치 않게 죽은 것이 아니라 **한 곳에서 오류 하나를 정상적으로 돌려주었고 그것을 전체 종료로 받아들인 것**이었습니다. 다만 그 한 곳이 아직 여러 동작을 함께 담고 있어 한 겹 더 좁힙니다.
- 이제 열두 단계에 각각 이름을 남깁니다. 연결을 붙이는 단계, 시간을 읽는 단계, 받은 내용을 저장하는 단계, 알림을 내보내는 단계 등이 따로 구분됩니다.
- 휴대폰 앱의 Java 쪽이 오류를 낼 때 어느 줄에서 났는지 남기지 않던 문제를 고쳤습니다. Java가 던진 오류는 종류를 불문하고 네이티브 쪽에 한 가지 값으로만 도착해서, 어느 기능이 실패했는지 알 수 없었습니다. 이제 오류가 만들어진 파일과 줄 번호를 남깁니다. 오류 문구 자체는 값이 섞여 들어갈 수 있으므로 남기지 않습니다.
- 다음 판에서 이 기록을 읽어 실제 수정을 넣습니다.
- 아래 항목은 이전 프리릴리즈의 변경 사항으로 계속 포함됩니다.

- 이 판은 진단판입니다. 동작은 한 줄도 바뀌지 않았고, 고장이 났을 때 어디서 났는지 기록이 말하게 만드는 것만 들어 있습니다.
- 바로잡습니다. 지난 판은 '실제 승인 요청이 도착하는 자리에서 휴대폰의 승인 처리가 통째로 멈추던 문제를 수정했다'고 적었지만, 그 수정은 휴대폰 앱의 Java 쪽만 고친 것이었습니다. 실제 휴대폰에 두 판을 모두 깔고 PC에서 진짜 승인 창을 띄워 보니, **1초도 지나지 않아 휴대폰의 승인 처리가 그대로 멈췄습니다.** 앱이 종료되지는 않으므로 겉으로는 살아 있어 보이지만, PC와의 연결이 끊긴 뒤 다시 붙지 못하고 앱을 완전히 닫았다 열어야만 돌아옵니다. 남은 원인은 그 아래 네이티브 쪽에 있습니다.
- 그 네이티브 쪽에는 오류 하나를 승인 처리 전체의 종료로 받아들이는 자리가 열 군데 있고, 어느 자리가 멈췄는지 말해 주는 기록이 없었습니다. 이제 열 자리마다 고정된 이름과 오류 종류를 남깁니다.
- 네이티브 작업이 예기치 않게 죽으면 아무 기록도 남지 않던 문제를 고쳤습니다. 기존 기록 통로는 앱 화면이 한 번이라도 열린 뒤에만 살아 있는데, 승인 처리는 화면 없이 도는 백그라운드에서 일어납니다. 이제 화면과 무관한 통로로 남깁니다. 남기는 것은 소스 파일 이름과 줄 번호뿐이고, 오류 문구 자체는 값이 섞여 들어갈 수 있으므로 남기지 않습니다.
- 다음 판에서 이 기록을 읽어 실제 원인을 고칩니다.
- 아래 항목은 이전 프리릴리즈의 변경 사항으로 계속 포함됩니다.

- 휴대폰이 PC와 연결된 뒤 실제 승인 요청이 도착하는 자리에서 휴대폰의 승인 처리가 통째로 멈추던 문제를 수정했습니다. 요청을 정리하는 과정이 예상 못 한 오류를 하나라도 만나면 첫 번에 전체를 접었고, 그 오류를 붙잡지 않고 버려서 기록에도 원인이 남지 않았습니다. 이제 감당할 수 없는 오류만 즉시 접고, 그 밖의 오류는 세면서 넘어가되 연속 네 번을 넘기면 접습니다. 넘어간 실패는 따로 기록해 실제로 처리를 멈춘 원인을 덮지 않습니다. 무엇이 그 오류를 냈는지는 아직 확인하지 못했고, 다음에 같은 자리에 닿으면 기록이 이름을 남깁니다.
- 휴대폰에서 누른 승인이 PC에서 조용히 거절되던 문제를 수정했습니다. PC는 승인 창을 처음 본 순간과 실제로 누르기 직전을 견주어 내용이 달라졌으면 누르지 않는데, 이 비교에 창의 내용뿐 아니라 보안 화면에 떠 있는 창의 총 개수까지 들어가 있었습니다. Windows는 승인 창이 떠 있는 동안 화면에 보이지도 않는 창을 스스로 열고 닫으므로, 휴대폰에서 결정하기까지 시간이 걸릴수록 승인 창은 그대로인데 개수만 달라져 거절되는 일이 생깁니다. 이제 승인 창 자체에 관한 것만 견줍니다. 창 내용, 글자, 단추, 요소 수를 견주는 검사와 대상 창이 여전히 그 창인지 확인하는 검사는 모두 그대로입니다. CI가 이 문제로 두 번 연속 멈춘 기록에서 원인을 확인했습니다.
- PC와 연결된 뒤에도 휴대폰 화면에 PC를 연결하라는 안내가 계속 나오던 문제를 고쳤습니다. 요청 화면은 연결 여부와 상관없이 이 안내를 그렸고, 알림 시간 화면은 목록을 읽지 못할 때마다 그렸습니다. 이제 휴대폰이 아는 PC가 하나라도 있으면 양쪽 화면에서 나오지 않습니다.
- 아래 항목은 이전 프리릴리즈의 변경 사항으로 계속 포함됩니다.

- 휴대폰에서 QR을 찍어 연결하는 도중 앱이 종료되던 문제를 수정했습니다. 지난 두 판이 이 문제를 고쳤다고 적었지만 실제로는 고쳐지지 않은 채 나갔습니다. 앱은 시작할 때마다 고쳤다는 기록을 남기고 있었는데, 그 설정을 실제로 읽는 시점은 이미 지나간 뒤였습니다. 설정을 쓰이기 전에 넣도록 순서를 바꿨고, 연결 작업 스레드 자체에도 같은 잠금을 따로 겁니다. 실제 휴대폰에서 연결을 세 번 시도해 세 번 모두 같은 자리에서 종료된 기록으로 원인을 확인했습니다. 그 뒤 실제 휴대폰에서 연결이 성공하는 것을 확인했습니다.
- PC 연결 화면의 단추가 나머지 화면과 동떨어진 회색 입체 사각형으로 나오던 문제를 고쳤습니다. 이 화면은 모든 요소를 직접 그리는데 단추만 Windows 기본 단추였고, 이 프로그램에는 단추를 최신 모양으로 그리게 하는 설정이 없어 옛 모양으로 나왔습니다. 이제 단추도 같은 방식으로 직접 그립니다. 단추의 글자와 동작은 바뀌지 않았습니다.
- 아래 항목은 이전 프리릴리즈의 변경 사항으로 계속 포함됩니다.

- 보안 수정: 휴대폰과 주고받는 암호화 연결에 쓰는 rustls를 0.23.45로 올렸습니다. 이전 판은 TLS 1.3 연결 과정에서 암호화되어야 할 메시지가 평문으로 와도 연결을 끊지 않았습니다(RUSTSEC-2026-0285). 이 문제로 연결 내용이 바뀌거나 남이 대신 연결을 맺을 수는 없지만, 규격이 끊으라고 정한 경우를 끊지 않았습니다.
- 의존성 보안 권고 검사를 CI에 추가했습니다. 위 문제도 이 검사를 처음 돌려서 찾았습니다.
- PC에서 QR이 갑자기 화면 전체를 덮던 방식을 바꿨습니다. 이제 무엇을 하는 절차인지, 휴대폰에서 무엇을 먼저 켜야 하는지, 어떻게 중지하는지를 먼저 안내하는 화면이 나오고, [QR 코드 보기]를 눌러야 QR이 보입니다.
- 안내 화면에 주의 문구를 넣었습니다. 다른 사람의 요청으로 이 절차에 들어왔다면 바로 중지하라는 내용과, QR 코드를 다른 사람에게 공유하지 말라는 내용입니다.
- QR 화면에서 나갈 수 없던 문제를 해결했습니다. [취소하고 돌아가기] 단추를 넣었고, ESC 키도 받습니다. 두 화면 모두에서 언제든 원래 화면으로 돌아갑니다. 비교 숫자가 나오기 전에 나가는 것은 아무것도 결정하지 않으며, 키나 등록을 만들지 않습니다.
- 남은 시간 표시를 '01:47 후 종료' 형태로 바꿨습니다. 남은 시간을 세는 대신 언제 끝나는지만 알립니다. 화면이 알아서 닫힌다는 안내는 통제권이 사용자에게 없다는 뜻으로 읽혀 넣지 않았습니다.
- 번역이 긴 언어에서 글자가 잘리던 문제를 함께 고쳤습니다. 비교 숫자 화면의 단추를 가장 긴 번역에 맞춰 키웠고, 왼쪽 위 이름이 카드에 가릴 만큼 길면 잘라서 보여주는 대신 표시를 생략합니다.
- 화면 왼쪽 위에 프로그램 표시와 이름을 한 번만 넣었습니다. QR 자체의 크기와 선명도는 바꾸지 않았습니다. 휴대폰 카메라와 CI 검사가 읽는 최소 크기를 화면 크기별로 지키는지 시험으로 확인합니다.
- 휴대폰에서 QR을 찍어 연결하는 도중 앱이 갑자기 종료되던 문제를 수정했다고 적었던 항목입니다. 연결 절차가 키 생성을 요청하고, 그 안에서 시간 확인이 다시 들어오는 순간 해당 작업 스레드가 실행 중에 분리되어 Android가 앱을 종료한다는 원인 파악은 맞았으나, 그 판의 수정은 효력이 없었습니다. 이번 판에서 실제로 고쳤습니다.
- 연결이 중단된 뒤 앱이 다시는 열리지 않던 문제를 수정했습니다. 저장이 중간에 끊겨 남은 기록은 내용을 확인한 뒤에만 정리하고, 마지막으로 확정된 상태를 그대로 엽니다. 확정된 저장 파일은 다시 쓰거나 새로 만들지 않으며, 알 수 없는 파일이 남아 있으면 정리하지 않고 멈춥니다.
- 키를 만들다 중단되어 남은 준비 기록 때문에 앱이 열리지 않던 문제를 수정했습니다. 그 준비에 해당하는 키만 지운 뒤 기록을 철회하며, 등록을 마친 키와 그 밖의 기록은 지우지 않습니다. 지우기가 끝나기 전에는 기록을 철회하지 않으므로 중단되면 다음 실행에서 다시 시도합니다.
- 앱 준비에 실패했을 때 최신 버전으로 업데이트하라고 안내하던 문구를 바꿨습니다. 업데이트로는 해결되지 않는 상황이므로, 앱을 완전히 닫았다가 다시 열도록 안내합니다.
- 아래 항목은 이전 프리릴리즈의 변경 사항으로 계속 포함됩니다.

- 승인 창에 닫기 단추가 함께 있으면 휴대폰이 보낸 결정을 Windows에 적용하지 못하던 문제를 수정했습니다. 예와 아니요를 뜻하는 단추가 각각 하나씩일 때만 누른다는 조건은 그대로이며, 그 밖의 단추는 어느 쪽도 뜻하지 않으므로 선택 대상이 되지 않습니다.
- 휴대폰에 표시되는 프로그램 이름이 승인 창의 제목과 아이콘으로 채워지던 문제를 수정했습니다. 이제 창이 실제로 가리키는 프로그램 이름이 들어갑니다. 승인 창을 펼치지 않으므로 파일 위치가 없는 경우가 있으며, 이때는 세부 정보에 그대로 남습니다.
- 휴대폰 연결이 성공한 직후 서비스가 그 연결 절차를 스스로 폐기해 기기가 등록되지 않던 문제를 수정했습니다. 등록을 요청하기 전의 검사는 모두 그대로 두고, 요청한 뒤에만 절차 유지 검사를 멈춥니다.
- PC 연결 준비에서 실행 파일의 네이티브 경로를 보유 중인 설치 파일과 매번 대조하도록 변경했습니다. 기존 파일 권한·파일 신원·프로세스 검사는 유지합니다.
- QR 표시 프로세스의 창 스테이션 검사에는 실제 사용하는 속성 조회 권한만 요청합니다. 실패 기록은 데스크톱과 창 스테이션 전달을 구분합니다.
- QR 화면의 독립 검사를 서비스가 만든 사용자 세션 내 검사 프로세스에서 수행하도록 변경했습니다. 연결 결정까지 화면 객체를 유지·재검사하며 기존 서비스의 프로세스·파일·토큰 검증도 유지합니다.
- QR 내용을 전달하기 전에 숨은 검증용 창이 원래 프로세스·스레드와 보호된 데스크톱에 속하는지 Windows에서 확인합니다. 확인용 창은 통신 종료까지 유지하며 기존 제한 권한을 사용합니다.
- 비교 숫자 화면에서 사용자 확인을 기다리는 정상 대기 상태를 오류로 처리해 창이 닫히던 문제를 수정했습니다.
- Android에서 연결·요청·기록이 없는 상태의 유지 관리와 조회가 불필요한 저장 작업을 만들지 않도록 변경했습니다. 기존 파일과 동기화 검사는 유지합니다. 실제 저장 도중 중단되어 이미 복구가 필요한 상태는 자동으로 지우거나 복구하지 않습니다.
- Windows CI가 실제 PC 앱에서 관리자 확인을 거쳐 QR을 열고, 별도 소프트웨어 휴대폰 픽스처가 화면의 QR을 읽어 연결하도록 시험을 확장했습니다.
- 양쪽 비교 확인, 서비스의 기기 등록, 실제 UAC 요청 전달, 같은 요청에 대한 서명된 거절 응답과 Windows 취소 결과를 모두 검사합니다. 시험 대상 프로그램이 실행되면 실패합니다.
- 이 연결 시험과 Android 릴리스 시작 시험이 해당 커밋에서 통과해야 프리릴리즈가 발행됩니다. 소프트웨어 픽스처는 실제 Android 하드웨어 인증이나 생체 인증을 대신하지 않습니다.
- Windows 연결 실패 단계와 원래 오류 코드를 제한된 진단 기록에 남깁니다. QR·비교 숫자·키·자격 증명은 기록하지 않습니다.
- 아래 항목은 이전 프리릴리즈의 변경 사항으로 계속 포함됩니다.

- QR 준비가 시작된 뒤에는 상태 새로고침이 같은 통신 자리를 먼저 차지하지 않도록 요청 순서를 조정했습니다.
- 이전 연결 작업이 실제로 종료되기 전에는 새 연결을 시작하지 않습니다. 오류 문구의 표시 시간이 끝나도 정리 중인 작업은 유지합니다.
- 작업 종료 확인이 실패했거나 종료 결과가 누락됐을 때, 다음 조회에서 성공 또는 진행 중 상태로 되돌아가던 처리를 수정했습니다.
- 이전 Windows CI는 실제 앱의 QR 버튼에서 Windows 승인 창까지 확인한 뒤 취소하는 범위였습니다. 이번 판부터 승인 후 QR 표시와 소프트웨어 픽스처의 연결·거절까지 검사합니다. 실제 휴대폰 등록과 원격 승인은 별도 사용자 시험 항목입니다.
- 아래 항목은 이전 프리릴리즈의 변경 사항으로 계속 포함됩니다.

- Windows 관리 앱이 서비스의 신원과 생존 여부를 확인할 때 필요한 프로세스 조회 권한을 서비스 시작 과정에서 설정합니다. 일반 사용자에게는 제한된 정보 조회와 종료 확인만 허용하며, 메모리 읽기·프로세스 제어 권한은 주지 않습니다.
- 중계 상태를 아직 읽지 못해도 외부 중계 주소를 작성할 수 있습니다. 저장 조건과 관리자 확인은 유지하며, 조회가 복구돼도 작성한 주소가 자동으로 저장되지는 않습니다.
- 기기 연결 해제 요청이 이전 관리 프로토콜 형식으로 생성되어 실패하던 오류를 수정했습니다.
- 아래 항목은 이전 시험 빌드에 포함된 변경 사항입니다.

- 관리 화면의 조회 연결이 끊기거나 거부될 때 휴대폰 승인 서비스 전체가 종료되던 처리를 수정했습니다. 해당 연결을 정리한 뒤 다음 조회를 받습니다.
- 내장 중계의 설정 여부와 실제 수신 상태를 구분해 표시합니다. 서비스 중지, 네트워크 대기, 상태 미확인, 외부 중계 설정을 각각 안내합니다.
- 휴대폰 승인 설정 변경이 실패하면 자동 새로고침 후에도 결과를 남깁니다. 시작 직후 다시 중지된 상태를 성공으로 처리하지 않습니다.
- Windows CI에 실제 보호된 관리 클라이언트와 반복 조회·거부된 연결을 추가했습니다. 기존 감시·중계 검사와 함께 서비스 유지 여부를 검사합니다.
- 아래 항목은 이전 프리릴리즈의 변경 사항으로 계속 포함됩니다.

- Android 뒤로가기 버튼·제스처를 앱 설정 창의 취소 동작에 연결했습니다. 적용하지 않은 언어 선택은 버리고 원래 화면으로 돌아갑니다.
- 저장 처리 중에는 뒤로가기로 앱을 나가지 않습니다. 설정 창이 없을 때는 기존 Android 뒤로가기 동작을 유지합니다.
- 아래 항목은 이전 프리릴리즈의 변경 사항으로 계속 포함됩니다.

- [다시 확인]과 언어 설정 버튼을 제목 줄 끝에 배치했습니다. 설명문이 길어지거나 탭을 바꿔도 버튼이 아래 줄로 밀리지 않습니다.
- 좁은 화면과 큰 글자에서는 새로고침을 아이콘으로 표시하며, 접근성 이름과 누르기 영역은 유지합니다. 설명문은 별도 줄 전체 너비를 사용합니다.
- 아래 항목은 이전 프리릴리즈의 변경 사항으로 계속 포함됩니다.

- 한국어에 영어·프랑스어·독일어·일본어·중국어(간체/번체)·스페인어·포르투갈어(브라질/포르투갈)·아랍어를 추가했습니다.
- 첫 실행은 시스템 언어를 따릅니다. 화면 위쪽의 [앱 설정]에서 언어를 바꾸거나 시스템 언어로 돌아갈 수 있습니다. 지원하지 않는 언어는 영어로 표시합니다.
- 앱 화면과 자체 제작한 네이티브 연결 화면에 언어별 글꼴을 포함했습니다. Windows UAC와 Android 본인 확인 등 운영체제 소유 화면은 운영체제 설정을 따릅니다.
- 프로그램·경로·명령행의 숨은 방향 제어 문자를 `[U+202E]`처럼 표시하고, 주변 아랍어 문장과 방향을 분리합니다. 서명과 다이제스트에 사용하는 원본 요청은 변경하지 않습니다.
- Android 알림에서 이름이나 경로가 생략되거나 표시 길이를 넘으면 상세 화면에서 확인하도록 안내합니다.
- 아래 항목은 이전 프리릴리즈의 수정 사항으로 계속 포함됩니다.

- Windows 백그라운드 서비스에 중계를 포함했습니다. 별도 중계 프로그램 없이 같은 네트워크에서 연결을 준비하며, 앱 창을 닫아도 서비스와 함께 실행됩니다.
- 설치 시 바탕화면·시작 메뉴·작업표시줄 선택을 받습니다. 새 설치는 선택, 업그레이드·복구는 선택 해제가 기본값이며 기존 바로가기는 유지합니다.
- 작업표시줄 고정은 설치 후 앱에서 Windows 확인을 거칩니다. 지원하지 않는 Windows에는 직접 고정하는 방법을 안내합니다.
- 내장 중계의 방화벽 허용은 해당 서비스·실행 파일의 TCP 7443, 개인 네트워크 프로필로 한정합니다. 외부 모바일망에는 PC에 도달하는 경로나 외부 중계가 필요합니다.
- 키 생성 확정 후 저장된 키를 다시 열어 검증하고, 이 핸들을 실제 실행에 사용합니다.
- 검증 실패 때 키 삭제를 시도하던 처리를 없애 키와 최초 오류를 보존합니다.
- 관리자 재설치에서 연결 정보 파일이 완전히 없는 중단 상태만 한 번 이어서 초기화할 수 있게 합니다. 기존 연결 정보 파일은 빈 파일이나 손상된 파일도 덮어쓰지 않습니다.
- Windows TPM 키의 생성 중 속성과 완성된 속성을 구분해 초기화를 검사합니다.
- TPM이 생성 중 반환하는 `ECDSA`, 길이 `0`, 키 종류 `0` 때문에 발생한 `0xE1000009` 거부를 수정합니다.
- 완성된 키의 256비트·컴퓨터 범위·TPM 제공자·접근 권한·서명 전용·비공개 키 내보내기 금지 검사는 유지합니다.
- 알고리즘 이름뿐 아니라 실제 P-256 공개키 형식과 곡선도 검사합니다.
- 이전 판의 Android QR 진입·알림 설정 수정은 유지됩니다.
- 기존 연결·인증 조건과 저장된 설정·기기 정보는 유지합니다.

이 판은 시험용 프리릴리즈입니다. 설치 파일과 APK를 만들었을 뿐, 실제 PC와 휴대폰에서
원격 승인이 동작했다는 증거는 아닙니다. 아래 표의 각 항목은 이 판에서 실제로 어디까지
검증했는지를 적은 것입니다.

## 내려받기

| 파일 | 용도 |
| --- | --- |
| `uac-remote-controller-windows-x64-setup.exe` | Windows 11 x64 설치 파일. 설치 때 관리자 확인이 한 번 필요합니다. |
| `uac-remote-controller-android-arm64.apk` | Android 11(API 30) 이상, arm64 휴대폰용 앱. |
| `uac-relay-windows-x64.exe`, `uac-relay-linux-x64` | 선택 사항인 외부 서버용 중계입니다. PC 내장 중계를 쓸 때는 별도로 실행하지 않습니다. |
| `SHA256SUMS.txt` | 위 파일들의 SHA-256. |

## 서명

- Android APK 서명 인증서 SHA-256: `{{SIGNER_SHA256}}` (출처: {{SIGNER_SOURCE}})
- Windows 설치 파일과 실행 파일은 코드 서명이 없습니다. SmartScreen 경고가 표시됩니다.

`ephemeral-this-run-only`라고 적혀 있으면 이 판을 만들 때 임시로 만든 서명 키입니다. 다음 판은
다른 키로 서명되므로 그대로 덮어 설치할 수 없고, 앱을 지운 뒤 설치해야 합니다.

## 검증 상태

{{CAPABILITY_TABLE}}

## 시작하기

1. Windows에 설치 파일을 실행하고 관리자 확인을 승인합니다. 설치가 끝나면 서비스가 부팅 때
   자동으로 시작되도록 등록됩니다.
2. 같은 네트워크에 PC와 휴대폰을 연결합니다. Windows의 네트워크 프로필은 ‘개인’이어야 합니다.
   내장 중계는 서비스와 함께 준비됩니다. 이전에 외부 중계를 설정했다면 앱의 [휴대폰 관리]에서 [이 PC의 내장 중계 사용]을 선택합니다.
3. 외부 모바일망으로 연결하려면 PC로 들어오는 연결 경로나 공개 주소의 외부 중계가 필요합니다.
   외부 중계 주소는 앱의 [고급 설정: 외부 중계 서버]에서 입력할 수 있습니다.
4. 휴대폰에 APK를 설치하고 화면 잠금(PIN, 패턴, 비밀번호, 생체)을 설정합니다.
5. PC의 UAC 원격 승인 앱에서 [휴대폰 관리] → [UAC 원격 승인]을 누르면 관리자 확인 뒤 QR이 보호된 화면에 표시됩니다.
   휴대폰 앱에서 [PC의 QR 코드 촬영]으로 QR을 읽고, 양쪽에 표시된 여섯 자리 숫자가 같은지 확인한 뒤
   양쪽에서 확인을 누릅니다.

## 체크섬

```text
{{SHA256SUMS}}
```

빌드 커밋: `{{COMMIT}}`
