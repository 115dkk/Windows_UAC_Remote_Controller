# Physical authentication latency and recovery

User priority: finish remaining instructions, actual phone authentication latency
not ping, connection recovery first, measured feedback UI next, verified deployment.

1. Network recovery: default-network events, prompt bounded reconnect, no VPN
   bypass/SSID authorization/re-pair/replay. Implementation exists; CI/security pending.
2. Actual timing: native action acceptance -> Android OS authentication success ->
   signing/preparation -> same-request committed authenticated PC result using
   one Android elapsed clock. Body-free bounded receipts drive observations.
   Implementation exists; ABI14 integration, review and CI pending.
3. Actual phone trial: update with same persistent APK signer; user performs own
   biometrics. Windows fixed cmd.exe /d /c exit0 canary verifies real application.
   First alpha1 probe yielded no verified phone approval or WindowsApplied record;
   elapsed122378ms is unconfirmed whole-wait duration, not authentication latency.
4. UI: implement proposed truthful selection/progress/result card after valid
   device measurement. No last-history-row inference or fabricated success.
5. Root-only validation: exact CI, static security reviewer, fresh final
   architecture review/ROOT approval, signed Windows+Android new prerelease.

External routability is a separate deployment prerequisite. Actual phone and PC
use private-LAN address; code recovery cannot make that address Internet-routable.
RunPod read-only inventory contains0 pods. Live CPU2 catalog minimum observed
compute price0.03USD/vCPU/hour x2 =0.06USD/hour, available EU regions, storage extra.
No resource created, no billable action/port publication/credential extraction.
Need user-approved existing public host or new hosting cost/operating scope for
actual mobile-data path. Other scoped native implementation continues independently.

No personal UAC/security policy changes, no app data/key deletion. Device netlink
inspection permission denial is not retried through escalation/private-state access.
Existing historical Claude handoff is complete and is not repeated.
