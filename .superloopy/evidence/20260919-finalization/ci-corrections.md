# Exact-commit CI correction record

ROOT-owned execution; no child ran validators.

- a16c12c: initial CI exposed incomplete branding/package expectations, raw versus
  translated UI assertions,21-obligation fixture updates and a watcher-call
  signature omission. Fixed in a2c192f. Android APK/JVM and notification gallery
  passed; not treated as final-commit proof.
- a2c192f: diagnostic admission still required16 rows although actual manifest
  now required21; admission now derives the exact complete set from the reviewed
  manifest, with independent bounds and all per-row/source checks preserved.
  WinUSB ProcessIdToSessionId import moved to its actual RemoteDesktop module.
  Obsolete service-word prohibition was corrected to retain the real no-false-
  remote-readiness invariant. Architecture C1 landed in89d8481.
-89d8481: real Android source-title/minified-startup label guards still named old
  UI copy. Updated exact expectations, not time budgets or readiness checks.
  Gallery139/141 failure was a remaining old confirmation sentence; corrected.
  Android-controller native epoch-change denial test incorrectly expected an
  old active request to remain; now asserts its exact withdrawal, no stale frame,
  no outcome/history and retained source markers. Production behavior unchanged.
-89d8481 actual Tamarin renewal property3 hit120s with clean SIGTERM, not a
  falsified proof or heap error. Other renewal properties and broken-protocol
  control passed. Search ranking and two freshly mandatory reuse helpers added;
  original transition/formula/mutant text preserved by erasure guard. CI rerun
  required; no timeout/inconclusive result counted as pass.

Separate actual installed alpha36 boot failure and cancelled start attempt are
recorded in installed-startup-failure.md. They are not consequences established
by these synthetic CI failures and remain causally unresolved.
